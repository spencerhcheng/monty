#include "monty.h"

/**
 * stack - returns a poitner to the first element in the stack
 * @head: pointer to first node in doubly linked stack_t list
 * @line_number: number of line in source Monty (.m) file
 * Return: void
 */

void stack(stack_t **head, unsigned int line_number)
{
	(void) head;
	(void) line_number;
}

/**
 * queue - returns a pointer to the last element in the stack
 * @head: pointer to first node in doubly linked stack_t list
 * @line_number: number of line in source Monty (.m) file
 * Return: void
 */

void queue(stack_t **head, unsigned int line_number)
{
	(void) head;
	(void) line_number;
}
